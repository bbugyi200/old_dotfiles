#!/usr/bin/env python

"""Done (completed) on-modify hook."""

import json
import sys

import done

old_task = json.loads(sys.stdin.readline())
new_task = json.loads(sys.stdin.readline())


if __name__ == "__main__":
    done.run(old_task, new_task)
    print(json.dumps(new_task))
