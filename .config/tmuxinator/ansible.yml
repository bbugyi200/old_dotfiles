# ~/.config/tmuxinator/ansible.yml

root: <%= @settings['root'] %>
name: ansible

on_project_start:
on_project_stop:
pre_window:

windows:
  - main:
      layout: even-horizontal
      panes:
        - 0:
            - ll
