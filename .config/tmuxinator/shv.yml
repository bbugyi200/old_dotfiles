# ~/.config/tmuxinator/shv.yml

root: <%= @settings['root'] %>
name: shv

on_project_start:
on_project_stop:
pre_window:

windows:
    - I:
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - II:
        layout: even-horizontal
        panes:
            - 0:
                - ll
