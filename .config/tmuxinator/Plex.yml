# ~/.config/tmuxinator/Plex.yml

root: <%= @settings['root'] %>
name: Plex

on_project_start:
on_project_stop:
pre_window:

windows:
    - I:
        layout: even-horizontal
        panes:
            - 0:
                - tsm-watch
    - II:
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - III:
        layout: even-horizontal
        panes:
            - 0:
                - ll
