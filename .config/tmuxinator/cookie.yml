# ~/.config/tmuxinator/cookie.yml

root: <%= @settings['root'] %>
name: cookie

on_project_start:
on_project_stop:
pre_window:

windows:
    - src:
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - tests:
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - templates:
        root: /home/bryan/.cookiecutters
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - config:
        root: /home/bryan/.config/cookie
        layout: even-horizontal
        panes:
            - 0:
                - ll
