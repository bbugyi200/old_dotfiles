# ~/.config/tmuxinator/es-prod`.yml

root: <%= @settings['root'] %>
name: es-prod`
socket_name: tmux_primes

on_project_start: es-prod-hook
pre_window:

windows:
    - vim:
        panes:
            - 0:
                - ll
