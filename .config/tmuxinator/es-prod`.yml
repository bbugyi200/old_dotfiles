# ~/.config/tmuxinator/es-prod`.yml

root: <%= @settings['root'] %>
name: es-prod`
socket_name: tmux_primes

on_project_start:
on_project_stop:
pre_window:

windows:
    - I:
        root: ~/projects/edgelp/prod
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - II:
        root: ~/projects/edgelp/prod
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - III:
        root: ~/projects/edgelp/prod
        layout: even-horizontal
        panes:
            - 0:
                - ll
