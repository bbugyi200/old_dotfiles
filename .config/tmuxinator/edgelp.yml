# ~/.config/tmuxinator/edgelp.yml

root: <%= @settings['root'] %>
name: edgelp

on_project_start: work-hook "edgelp"
on_project_stop: umount_encfs "edgelp"
pre_window:

windows:
  - vim:
      panes:
        - 0:
            - ll
