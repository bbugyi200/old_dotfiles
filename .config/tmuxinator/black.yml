# ~/.config/tmuxinator/black.yml

root: <%= @settings['root'] %>
name: black

on_project_start:
on_project_stop:
pre_window:

windows:
    - vim:
        layout: even-horizontal
        panes:
            - 0:
                - ll
