# ~/.config/tmuxinator/funky.yml

root: <%= @settings['root'] %>
name: funky

on_project_stop:

windows:
    - vim:
        layout: even-horizontal
        panes:
            - 0:
                - ll
