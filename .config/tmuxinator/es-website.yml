# ~/.config/tmuxinator/es-website.yml

root: <%= @settings['root'] %>
name: es-website

on_project_start:
on_project_stop:
pre_window:

windows:
    - vim:
        layout: even-horizontal
        panes:
            - 0:
                - ll
