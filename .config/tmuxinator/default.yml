# ~/.config/tmuxinator/<%= name %>.yml

root: <%%= @settings['root'] %>
name: <%= name %>

on_project_stop:

windows:
    - I:
        layout: even-horizontal
        panes:
            - 0:
                - lls
    - II:
        layout: even-horizontal
        panes:
            - 0:
                - lls
    - III:
        layout: even-horizontal
        panes:
            - 0:
                - lls
