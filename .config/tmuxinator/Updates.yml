# ~/.config/tmuxinator/Updates.yml

root: <%= @settings['root'] %>
name: Updates

on_project_start:
on_project_stop:
pre_window:

windows:
    - main:
          layout: even-horizontal
          panes:
              - 0:
                    - header "emanage -h"
