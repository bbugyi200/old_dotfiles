# ~/.config/tmuxinator/es-prod.yml

root: <%= @settings['root'] %>
name: es-prod

on_project_start: es-prod-hook
on_project_stop: umount_edgelp
pre_window:

windows:
    - vim:
        panes:
            - 0:
                - ll
