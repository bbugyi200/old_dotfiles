# ~/.config/tmuxinator/Terminal.yml

root: <%= @settings['root'] %>
name: Terminal

on_project_stop:

windows:
    - main:
        root: ~/
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - bin:
        root: ~/Dropbox/bin
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - home:
        root: ~/Dropbox/home
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - projects:
        root: ~/projects
        layout: even-horizontal
        panes:
            - 0:
                - ll
