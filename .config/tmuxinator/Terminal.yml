# ~/.config/tmuxinator/Terminal.yml

root: <%= @settings['root'] %>
name: Terminal

on_project_stop:

windows:
    - main:
        root: /home/bryan
        layout: even-horizontal
        panes:
            - 0:
                - ll
