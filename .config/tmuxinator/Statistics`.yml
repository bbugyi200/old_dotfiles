# ~/.config/tmuxinator/Statistics`.yml

root: <%= @settings['root'] %>
name: Statistics`
socket_name: tmux_primes

on_project_start:
on_project_stop:
pre_window:

windows:
    - HW:
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - Misc:
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - ThinkStats:
        root: /home/bryan/projects/ThinkStats2/code
        layout: even-horizontal
        panes:
            - 0:
                - ipython -i ipysession.py
