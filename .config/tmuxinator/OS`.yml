# ~/.config/tmuxinator/OS`.yml

root: <%= @settings['root'] %>
name: OS`
socket_name: primes

on_project_start:
on_project_stop:
pre_window:

windows:
    - src:
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - debug:
        layout: even-horizontal
        panes:
            - 0:
                - ll
    - tests:
        layout: even-horizontal
        panes:
            - 0:
                - ll
