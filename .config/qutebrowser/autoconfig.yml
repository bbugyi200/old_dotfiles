# DO NOT edit this file by hand, qutebrowser will overwrite it.
# Instead, create a config.py - see :help for details.

config_version: 1
global:
  bindings.commands:
    normal:
      C-r: restart
      ctrl-R: restart
  hints.auto_follow: unique-match
  hints.auto_follow_timeout: 2
  tabs.position: left
  tabs.show: always
